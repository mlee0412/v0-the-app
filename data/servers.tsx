// If there are duplicate server definitions in this file, ensure each server has a unique ID
// Make sure the server data is defined only once

// Example fix (if needed):
// Replace any duplicate server definitions with unique ones
// For example, if you have multiple entries like:
// { id: "1", name: "Diego", ... },
// { id: "1", name: "Diego", ... },
//
// Change to:
// { id: "1", name: "Diego", ... },
// { id: "2", name: "Gun", ... },
// etc.
